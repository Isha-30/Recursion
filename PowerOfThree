//Check if a given number n is a power of 4
class Solution {
    public boolean isPowerOfThree(int n) {
       if(n==1)return true;
        else if(n==0 || n%3 != 0)return false;
        else return isPowerOfThree(n/3);
        
    }
}
